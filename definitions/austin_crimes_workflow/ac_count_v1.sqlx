config {
    type: "table",
    name: "crime_count",
    description: "Stores the count of each type of crime committed in each district",
    tags: ["eda"]
}

SELECT
  primary_type,
  district,
  COUNT(*) AS cases_count
FROM
  ${ref("crime")}
  GROUP BY district, primary_type ORDER BY primary_type
